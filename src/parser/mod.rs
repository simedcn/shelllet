pub mod parser;
mod start_process;
mod sequence;

// use std::fs::File;
// use std::io::BufReader;
// use xml::reader::EventReader;

//  trait Parser {

//     fn name(self) -> &'static str;

//     fn parse(self, output: &mut String, parser : &mut EventReader<BufReader<File>>);
// }

